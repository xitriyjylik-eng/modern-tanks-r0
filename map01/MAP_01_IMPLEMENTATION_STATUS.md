# MAP 01 — статус реализации

Дата: 2026-09-10

## Выполнено
- создана отдельная ветка `map01-implementation-v1`;
- создана production-структура Map 01;
- размер master: 4096×3072;
- сектор: 256×256;
- сетка: 16×12 = 192 сектора;
- зафиксированы 11 логических зон с polygon bounds;
- зафиксированы 12 дорожных связей, 4 region exits и ключевые landmarks;
- определены схемы WORLD_ART / TERRAIN / COLLISION / OBJECTS / EVENTS / SPAWN;
- создан streaming contract;
- в полном проектном архиве созданы 192 отдельных sector metadata JSON и автоматический validator;
- validator структуры: PASS, errors=0, warnings=0.

## Z01 — runtime-интеграция
- данные первого участка Z01 подключены к реальному SGDK-коду через `map01_runtime.c/.h` и `map01_z01_data.c/.h`;
- `STATE_TEST_BATTLE` получает старт через `MAP01_Z01_getPlayerStart()`; подтверждённый старт: world `(900,564)`;
- логика использует Z01 TERRAIN/COLLISION и границы участка;
- `main.c`, `map01_runtime.c` и `map01_z01_data.c` успешно скомпилированы и слинкованы SGDK 2.11;
- собран отдельный тестовый ROM: 262144 bytes, SHA-256 `269503c83f2abbc05519e0c35226ccd2a54cde04bbeb77fc055f5c04a3811b9e`;
- ROM audit: PASS_WITH_WARNINGS, критических ошибок нет;
- GitHub Actions run `34449801871`: SUCCESS;
- BlastEm U/NTSC smoke: PASS по Map01 gate;
- движение камеры подтверждено: изменение игрового поля ~83.99% вправо и ~83.00% вниз;
- неподвижность HUD подтверждена: изменение ~0.271% вправо и ~0.195% вниз; нижняя панель 0%;
- возврат из боя в меню подтверждён: отличие от ранее снятого меню ~0.731%, допустимо из-за фоновой анимации меню.

## Что ещё не выполнено
- утверждённый WORLD_ART первой карты ещё не подключён к игровому рендереру;
- игровой экран Z01 пока использует старый R3 background как временный визуальный placeholder;
- полный WORLD_ART всей Map 01 ещё не разбит/подключён к потоковому рендереру;
- объекты, события и spawn остальных участков карты ещё не доведены до production-состояния.

## Следующий этап
Заменить временный R3 background в Z01 на реальный утверждённый WORLD_ART через потоковую подгрузку окна вокруг камеры. Не пытаться держать всю карту в VRAM одновременно; подгружать только необходимую видимую область и новые строки/столбцы при движении камеры. География и графика остаются статическими авторскими данными — без процедурной генерации мира.

## Ограничение ветки
`main` не изменять и не сливать до отдельной проверки нового WORLD_ART в эмуляторе и явного принятия пользователем.
